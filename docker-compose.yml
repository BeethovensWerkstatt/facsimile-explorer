version: "3"

services:
  front:
    build: nginx-ghcred
    ports:
      - 8080:8080
    volumes:
      - ./docker-nginx.conf:/etc/nginx/conf.d/default.conf
    environment:
      - CLIENT_ID=${CLIENT_ID}
      - CLIENT_SECRET=${CLIENT_SECRET}
    depends_on:
      - "webpack"
  webpack:
    image: node
    volumes:
      - ./:/usr/app/
    working_dir: /usr/app/
    command: "npm run serve"
